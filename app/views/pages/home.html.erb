<%= stylesheet_link_tag "/home.css" %>

	<% if user_signed_in? %>
	<div class= "welcome">Welcome <%= current_user.firstName %>!</div>
		<% if current_user.type == "Staff" %>
			<a href="/addUser"> Add User to System</a>
		<%end%>
		<% if current_user.type == "Requester" %>
			<a href="/applications/new"> Create Application</a>
			<a href="/applications">View My Applications</a>
		<%end%>
		<% if current_user.type == "Supervisor" %>
			<a href="/applications">Make Reccomendation</a>
		<%end%>
	<%end%>

<body class="home"></body>